# Coze Shopify 订单查询插件配置指南 订单查询已包含物流信息（fulfillments），但工作流中单独调用 getOrderTracking 是为了获取更丰富的翻译和话术模板

## 一、API 清单

共 **3 个 API**，全部支持多站点自动识别：

| API | 功能 | 输入参数 | 使用场景 |
|-----|------|---------|---------|
| `searchOrderByNumber` | 按订单号搜索订单（全站点） | `q`: 订单号 | ⭐ 常用 - 用户提供订单号时 |
| `getOrdersByEmail` | 按邮箱查询订单列表（全站点） | `email`: 邮箱 | ⭐ 常用 - 用户提供邮箱时 |
| `getOrderTracking` | 获取物流追踪信息 | `order_id`: Shopify内部ID | ⭐ 常用 - 查询物流状态 |

**支持的站点（9个）**：US、UK、EU、DE、FR、IT、ES、NL、PL

**自动识别规则**：
- 订单号前缀 UK → 英国站
- 订单号前缀 DE → 德国站
- 订单号前缀 EU → 欧洲站
- 订单号前缀 FR → 法国站
- 订单号前缀 IT → 意大利站
- 订单号前缀 ES → 西班牙站
- 订单号前缀 NL → 荷兰站
- 订单号前缀 PL → 波兰站
- 纯数字或无前缀 → 遍历所有站点查找

---

## 二、意图分类（5个）

| 意图 | 触发条件 | 调用API | 说明 |
|------|---------|---------|------|
| **order_query** | 用户提供了订单号 | searchOrderByNumber → getOrderTracking | 查订单详情+物流 |
| **order_list** | 用户提供了邮箱，没有订单号 | getOrdersByEmail | 查该客户的订单列表 |
| **order_guide** | 想查订单但没有提供订单号和邮箱 | 无 | 引导用户提供信息 |
| **product_consult** | 咨询Fiido产品相关问题 | 知识库检索 | 产品咨询 |
| **general_chat** | 闲聊、问候、无关问题 | 无 | 通用回复 |

---

## 三、Coze 工作流节点配置

### 整体流程图

```
                    ┌─────────────┐
                    │    开始     │
                    │  (用户输入)  │
                    └──────┬──────┘
                           ↓
                    ┌─────────────┐
                    │ 节点1: 意图识别 │
                    │   (LLM节点)   │
                    └──────┬──────┘
                           ↓
                    ┌─────────────┐
                    │ 节点2: JSON解析 │
                    │ (Deserialization) │
                    └──────┬──────┘
                           ↓
                    ┌─────────────┐
                    │ 节点3: 选择器  │
                    │  (分支节点)   │
                    └──────┬──────┘
        ┌──────┬──────┬────┴────┬──────────┐
        ↓      ↓      ↓         ↓          ↓
   order_   order_  order_   product_   general_
   query    list    guide    consult    chat
        ↓      ↓      ↓         ↓          ↓
   ┌────┴───┐  │      │    ┌────┴────┐     │
   │订单查询 │  │      │    │知识库检索│     │
   └────┬───┘  │      │    └────┬────┘     │
        ↓      ↓      ↓         ↓          ↓
   ┌────┴───┐  │      │    ┌────┴────┐     │
   │物流查询 │  │      │    │智能回复  │     │
   └────┬───┘  │      │    └────┬────┘     │
        ↓      ↓      ↓         ↓          ↓
   ┌────┴────────┴──────┴─────────┴──────────┴───┐
   │              格式化回复 (LLM)                 │
   └─────────────────────┬───────────────────────┘
                         ↓
                    ┌─────────┐
                    │   结束   │
                    └─────────┘
```

---

### 节点1: 意图识别（LLM节点）

**节点名称**: `意图识别`

**Prompt**: 参考 `prompts/意图识别.md`

**输出格式（JSON）**:
```json
{
  "intent": "order_query|order_list|order_guide|product_consult|general_chat",
  "sub_intent": "product_info|product_compare|product_price|product_purchase|product_aftermarket|product_usage|null",
  "order_number": "提取的订单号或null",
  "email": "提取的邮箱或null",
  "confidence": 0.95,
  "reason": "判断理由"
}
```

**输出变量**: `intent_output`

---

### 节点2: JSON Deserialization

**节点名称**: `JSON解析`

**输入**: `{{intent_output}}`

**输出变量**: `parsed`

**输出字段**:
| 字段 | 类型 | 说明 |
|------|------|------|
| `intent` | String | 意图类型 |
| `sub_intent` | String | 子意图（产品咨询用） |
| `order_number` | String | 订单号 |
| `email` | String | 邮箱 |
| `confidence` | Number | 置信度 |
| `reason` | String | 判断理由 |

---

### 节点3: 选择器（分支节点）

**分支条件**:

| 分支 | 条件 | 走向 |
|------|------|------|
| 分支1 | `{{parsed.intent}} == "order_query"` | → 订单查询节点 |
| 分支2 | `{{parsed.intent}} == "order_list"` | → 邮箱查询节点 |
| 分支3 | `{{parsed.intent}} == "order_guide"` | → 引导回复节点 |
| 分支4 | `{{parsed.intent}} == "product_consult"` | → 产品咨询节点 |
| 默认 | 其他情况 | → 通用回复节点 |

---

### 分支1: order_query（订单查询流程）

#### 节点4A: 订单查询（插件节点）

**节点名称**: `订单查询`

**调用工具**: `searchOrderByNumber`

**输入参数**:
```
q: {{parsed.order_number}}
```

**输出变量**: `order_result`

**说明**: API 会根据订单号前缀（UK/DE/EU/FR/IT/ES/NL/PL）自动识别站点

#### 节点4B: 物流查询（插件节点）

**节点名称**: `物流查询`

**调用工具**: `getOrderTracking`

**输入参数**:
```
order_id: {{order_result.data.order.order_id}}
```

**输出变量**: `tracking_result`

#### 节点4C: 订单回复（LLM节点）

**Prompt**:
```
根据订单和物流数据，用英文生成友好的客服回复。

## 订单数据
{{order_result}}

## 物流数据
{{tracking_result}}

## 回复要求
1. 使用友好专业的英文口吻
2. 突出订单状态和物流信息
3. 如果有运单号，提供给用户
4. 如果未发货，告知会尽快安排

## 回复模板
Hi! Here's the information for your order {{order_number}}:

📦 Order Status: {{fulfillment_status}}
💰 Total: {{total_price}} {{currency}}
📅 Order Date: {{created_at}}

🚚 Shipping Status: {{shipping_status}}
Carrier: {{tracking_company}}
Tracking Number: {{tracking_number}}

If you have any other questions, feel free to ask!
```

---

### 分支2: order_list（邮箱查询流程）

#### 节点5A: 邮箱查询（插件节点）

**节点名称**: `邮箱查询`

**调用工具**: `getOrdersByEmail`

**输入参数**:
```
email: {{parsed.email}}
limit: 5
```

**输出变量**: `order_list_result`

**说明**: API 会自动遍历所有 9 个站点，汇总该邮箱的所有订单

#### 节点5B: 订单列表回复（LLM节点）

**Prompt**:
```
根据订单列表数据，用英文生成友好的客服回复。

## 订单列表数据
{{order_list_result}}

## 回复要求
1. 列出所有订单的基本信息
2. 突出订单号、金额、状态
3. 询问用户需要查询哪个订单的详情

## 回复模板
Hi! Here are the orders associated with your email:

📦 Order {{order_number}}
   Amount: {{total_price}} {{currency}}
   Status: {{fulfillment_status}}
   Date: {{created_at}}

Which order would you like to know more about?
```

---

### 分支3: order_guide（引导回复）

**节点名称**: `引导回复`

**类型**: 文本输出 或 LLM节点

**输出内容**:
```
Hi! I can help you check your order status.

Please provide one of the following:
1. 📝 Order number (e.g., UK22080, DE10019, EU10019)
2. 📧 Email address used for the order

With this information, I can look up your order and shipping details!
```

---

### 分支4: product_consult（产品咨询流程）

#### 节点6A: 知识库检索（Knowledge节点）

**知识库**: Fiido 产品知识库

#### 节点6B: 智能回复（LLM节点）

**Prompt**: 参考 `prompts/1.md` 中的完整角色设定

---

### 分支5: general_chat（通用回复）

**节点名称**: `通用回复`

**Prompt**:
```
你是 Fiido 智能客服。用户进行了闲聊或问候。

用友好的方式回应，并引导用户咨询产品或订单问题。

用户输入: {{user_input}}
```

---

## 四、测试用例

### 测试1: UK站订单号查询
```
用户: 帮我查一下订单UK22080
预期意图: order_query
预期流程: searchOrderByNumber(q="UK22080") → getOrderTracking → 返回订单详情和物流信息
预期: 自动识别为UK站点
```

### 测试2: DE站订单号查询
```
用户: Check order DE10019
预期意图: order_query
预期流程: searchOrderByNumber(q="DE10019") → getOrderTracking → 返回订单详情
预期: 自动识别为DE站点
```

### 测试3: EU站订单号查询
```
用户: 订单EU10019的物流到哪了
预期意图: order_query
预期流程: searchOrderByNumber → getOrderTracking → 返回物流详情
预期: 自动识别为EU站点
```

### 测试4: 邮箱查询（全站点）
```
用户: 我的邮箱是 customer@example.com，查一下我的订单
预期意图: order_list
预期流程: getOrdersByEmail → 返回所有站点的订单列表
预期: 自动遍历9个站点汇总订单
```

### 测试5: 无信息查询
```
用户: 我想查订单
预期意图: order_guide
预期流程: 返回引导信息，请用户提供订单号或邮箱
```

### 测试6: 产品咨询
```
用户: C11 Pro 续航多少公里
预期意图: product_consult
预期流程: 知识库检索 → 智能回复
```

### 测试7: 闲聊
```
用户: 你好
预期意图: general_chat
预期流程: 通用回复
```

---

## 五、注意事项

1. **order_id vs order_number**
   - `order_number`: 用户看到的订单号（如 UK22080、DE10019）
   - `order_id`: Shopify内部ID（如 6615015620909）
   - 物流查询需要 `order_id`，所以必须先通过订单号搜索获取

2. **API 调用链路**
   - 订单查询: `searchOrderByNumber` → `getOrderTracking`
   - 邮箱查询: `getOrdersByEmail`（直接返回所有站点订单列表）

3. **多站点自动识别**
   - 订单号查询: 根据前缀自动识别站点（UK/DE/EU/FR/IT/ES/NL/PL）
   - 邮箱查询: 自动遍历所有9个站点，汇总订单
   - 用户无需指定站点，系统自动处理

4. **错误处理**
   - 订单不存在: API返回 `order: null`，提示用户检查订单号
   - 邮箱无订单: API返回空列表，提示用户确认邮箱

5. **JSON解析节点**
   - 必须放在意图识别LLM之后、选择器之前
   - 用于将LLM输出的JSON字符串解析为结构化对象
   - 解析后的字段可在后续节点中直接引用

6. **语言适配**
   - Fiido 面向全球市场，回复默认使用英文
   - 可根据用户输入语言自动切换

---

**文档版本**: v3.0
**更新时间**: 2025-12-15
**更新内容**: 从UK单站点升级为全站点自动识别（9个站点）
